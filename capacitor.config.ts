import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.garbaton.integradora',
  appName: 'Garbatón',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
